<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ landing.documentTitle }}</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600;700&display=swap"
    rel="stylesheet"
  />
  <link rel="stylesheet" href="styles.css" />
</head>
<body class="landing">
  <main class="landing__wrap" aria-labelledby="site-title">
    <div class="landing__frame">
      <span class="landing__tag">Normal Rooms</span>
      <h1 id="site-title">{{ landing.heading }}</h1>
      <p class="landing__copy">{{ landing.description }}</p>
      <a class="button landing__cta" href="home.html">{{ landing.ctaLabel }}</a>
    </div>
  </main>
</body>
</html>
